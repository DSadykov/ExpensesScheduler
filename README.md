# ExpensesScheduler 💰📅

**ExpensesScheduler** — это микросервисное Web API для управления личными финансами, построенное на современном технологическом стеке. Система позволяет планировать будущие траты, отслеживать текущие расходы, анализировать финансовые привычки и получать уведомления о предстоящих платежах.

## 📋 Содержание

- [Основные возможности](#-основные-возможности)
- [Архитектура](#-архитектура)
- [Технологический стек](#-технологический-стек)
- [Структура проекта](#-структура-проекта)
- [Требования](#-требования)
- [Быстрый старт](#-быстрый-старт)
- [Настройка переменных окружения](#-настройка-переменных-окружения)
- [Развертывание через Docker Compose](#-развертывание-через-docker-compose)
- [API документация](#-api-документация)
- [Примеры использования](#-примеры-использования)
- [Особенности реализации](#-особенности-реализации)

## ✨ Основные возможности

- **Планирование транзакций**: Создание запланированных платежей (аренда, подписки, кредиты) с различными периодами повторения (ежедневно, еженедельно, ежемесячно, разово)
- **Автоматический учет**: Автоматический учет запланированных платежей в истории при наступлении срока
- **История операций**: Удобный просмотр всех прошедших и будущих транзакций с фильтрацией по датам
- **Уведомления**: Автоматические напоминания о предстоящих платежах через email
- **Кэширование**: Оптимизация производительности через Redis кэширование
- **Безопасность**: JWT-аутентификация и авторизация пользователей

## 🏗 Архитектура

Проект построен на принципах микросервисной архитектуры с использованием асинхронной коммуникации через Apache Kafka.

### Компоненты системы

```
┌─────────────────┐
│   API Gateway   │ (YARP Reverse Proxy)
│   Port: 8082    │
└────────┬────────┘
         │
    ┌────┴────┬────────────────┐
    │         │                │
┌───▼───┐ ┌──▼──────┐   ┌──────▼─────┐
│ Auth  │ │Scheduler│   │Notification│
│ Service│ │ Service │   │  Service   │
│ 7069  │ │  7236   │   │   8081     │
└───┬───┘ └───┬─────┘   └──────┬─────┘
    │         │                │
    │    ┌────┴────┐           │
    │    │  Kafka  │◄──────────┘
    │    │  9092   │
    │    └────┬────┘
    │         │
┌───▼─────────▼────┐
│ SMTP Notification│
│    Service 8080  │
└──────────────────┘
```

### Поток данных

**1. Регистрация пользователя:**
```
Client → Gateway → AuthorizationService → SQL Server (Users DB)
                                    ↓
                            Kafka (NewUserCreatedMessage)
                                    ↓
                            NotificationService → Kafka (SmtpNewUserCreatedMessage)
                                    ↓
                            SmtpNotificationService → Email
```

**2. Планирование расходов:**
```
Client → Gateway → ExpensesSchedulerService → SQL Server (ExpensesDb)
                                          ↓
                                    Redis Cache
```

**3. Фоновая обработка уведомлений (ежедневно в 6:00 UTC):**
```
Hangfire → ScheduledExpensesWorker → SQL Server (получение запланированных расходов)
                                  ↓
                          Kafka (NotificationMessage)
                                  ↓
                          NotificationService → Kafka (SmtpNotificationMessage)
                                  ↓
                          SmtpNotificationService → Email отправка
                                  ↓
                          SQL Server (обновление истории + кэш)
```

### Микросервисы

1. **Gateway Service** (Порт: 8082)
   - Единая точка входа для всех клиентских запросов
   - Маршрутизация на основе YARP Reverse Proxy
   - Маршруты:
     - `/Authorization/{**}` → AuthorizationService
     - `/ExpensesScheduler/{**}` → ExpensesSchedulerService

2. **Authorization Service** (Порт: 7069)
   - Регистрация и аутентификация пользователей
   - Генерация JWT токенов
   - Хеширование паролей (BCrypt)
   - База данных: `Users`
   - Публикация событий в Kafka при регистрации

3. **Expenses Scheduler Service** (Порт: 7236)
   - Управление запланированными расходами
   - История операций
   - Кэширование через Redis
   - Фоновые задачи через Hangfire
   - Базы данных: `ExpensesDb`, `Hangfire`
   - Публикация уведомлений в Kafka

4. **Notification Service** (Порт: 8081)
   - Обработка уведомлений из Kafka
   - Перенаправление в SMTP сервис
   - Маршрутизация сообщений

5. **SMTP Notification Service** (Порт: 8080)
   - Отправка email-уведомлений
   - Управление настройками уведомлений пользователей
   - База данных: `SmtpNotificationSettings`

6. **Web Service** (Порт: 3000)
   - Веб-интерфейс на базе nginx
   - Статический фронтенд (HTML/CSS/JavaScript)
   - Проксирование API запросов к Gateway
   - Доступен по адресу: `http://localhost:3000`

7. **Shared Libraries**
   - **ExpensesScheduler.Authorization**: Общая логика авторизации (JWT Middleware, Attributes)
   - **ExpensesScheduler.Messaging**: Инфраструктура Kafka (Producers/Consumers)

## 🛠 Технологический стек

### Backend
- **.NET 10.0** / **C#** / **ASP.NET Core**
- **Entity Framework Core** - ORM для работы с базами данных
- **Hangfire** - Фоновые задачи и планировщик
- **YARP** (Yet Another Reverse Proxy) - API Gateway

### Базы данных и кэширование
- **SQL Server 2022** - Реляционная база данных
- **Redis** - Распределенное кэширование
- **Entity Framework Core** - Миграции и работа с БД

### Очереди сообщений
- **Apache Kafka 7.4.0** (KRaft mode, без Zookeeper) - Асинхронная коммуникация между сервисами

### Инфраструктура
- **Docker** / **Docker Compose** - Контейнеризация и оркестрация
- **Health Checks** - Мониторинг состояния сервисов

## 📁 Структура проекта

```
ExpensesScheduler/
├── ExpensesScheduler.Gateway/              # API Gateway (YARP)
├── ExpensesScheduler.AuthorizationService/ # Сервис авторизации
├── ExpensesScheduler.ExpensesSchedulerService/ # Основной сервис планирования расходов
├── ExpensesScheduler.NotificationService/  # Сервис уведомлений
├── ExpensesScheduler.SmtpNotificationService/ # SMTP сервис
├── ExpensesScheduler.Web/                  # Веб-интерфейс (nginx + static files)
├── ExpensesScheduler.Authorization/        # Библиотека авторизации (shared)
├── ExpensesScheduler.Messaging/            # Библиотека Kafka (shared)
├── docker-compose.yml                      # Конфигурация Docker Compose
└── README.md
```

## 📋 Требования

- **Docker Desktop** (или Docker Engine + Docker Compose)
  - Минимум 8 GB RAM (рекомендуется 16 GB)
  - Доступные порты: 8082, 7069, 7236, 8081, 8080, 3000, 9092, 6379, 1434
- **.NET SDK 10.0** (только для локальной разработки без Docker)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd denis_scheduler
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта (или экспортируйте переменные окружения):

```bash
# SQL Server
MSSQL_SA_PASSWORD=YourStrong!Password123
SQLSERVER_PORT=1434

# JWT Secret (минимум 32 символа для HMAC-SHA256)
JWT_SECRET=YourSuperSecretJwtKey12345678901234560123456

# Kafka
KAFKA_PORT=9092

# Redis
REDIS_PORT=6379

# Gateway
GATEWAY_SERVICE_PORT=8082

# Web Frontend
WEB_SERVICE_PORT=3000

# Environment
ASPNETCORE_ENVIRONMENT=Development
```

**Важно**: 
- `JWT_SECRET` должен быть минимум **32 символа** (256 бит для алгоритма HMAC-SHA256)
- `MSSQL_SA_PASSWORD` должен соответствовать требованиям сложности SQL Server (минимум 8 символов, включая заглавные, строчные, цифры и специальные символы)

### 3. Запуск через Docker Compose

```bash
docker-compose up -d
```

Эта команда:
- Соберет Docker образы для всех сервисов
- Запустит все необходимые контейнеры (SQL Server, Kafka, Redis, сервисы)
- Настроит сети и зависимости
- Инициализирует базы данных

### 4. Проверка статуса

Проверьте, что все контейнеры запущены:

```bash
docker-compose ps
```

Проверьте логи сервисов:

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f gateway-srv
docker-compose logs -f auth-srv
docker-compose logs -f scheduler-srv
```

### 5. Проверка Health Checks

```bash
# Gateway
curl http://localhost:8082/health

# Authorization Service
curl http://localhost:7069/health

# Expenses Scheduler Service
curl http://localhost:7236/health

# Notification Service
curl http://localhost:8081/health

# SMTP Notification Service
curl http://localhost:8080/health
```

## ⚙️ Настройка переменных окружения

### Вариант 1: Файл .env (рекомендуется)

Создайте файл `.env` в корне проекта:

```env
MSSQL_SA_PASSWORD=YourStrong!Password123
JWT_SECRET=YourSuperSecretJwtKey12345678901234560123456
SQLSERVER_PORT=1434
KAFKA_PORT=9092
REDIS_PORT=6379
GATEWAY_SERVICE_PORT=8082
WEB_SERVICE_PORT=3000
ASPNETCORE_ENVIRONMENT=Development
```

Docker Compose автоматически загрузит переменные из `.env` файла.

### Вариант 2: Экспорт переменных (Linux/Mac)

```bash
export MSSQL_SA_PASSWORD="YourStrong!Password123"
export JWT_SECRET="YourSuperSecretJwtKey123456789"
export SQLSERVER_PORT=1434
export KAFKA_PORT=9092
export REDIS_PORT=6379
export GATEWAY_SERVICE_PORT=8082
export ASPNETCORE_ENVIRONMENT=Development
```

### Вариант 3: PowerShell (Windows)

```powershell
$env:MSSQL_SA_PASSWORD="YourStrong!Password123"
$env:JWT_SECRET="YourSuperSecretJwtKey123456789"
$env:SQLSERVER_PORT=1434
$env:KAFKA_PORT=9092
$env:REDIS_PORT=6379
$env:GATEWAY_SERVICE_PORT=8082
$env:ASPNETCORE_ENVIRONMENT="Development"
```

## 🐳 Развертывание через Docker Compose

### Запуск всех сервисов

```bash
docker-compose up -d
```

### Остановка всех сервисов

```bash
docker-compose down
```

### Остановка с удалением volumes (удаление всех данных)

```bash
docker-compose down -v
```

### Пересборка образов

```bash
docker-compose build --no-cache
docker-compose up -d
```

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f auth-srv
docker-compose logs -f scheduler-srv
docker-compose logs -f gateway-srv
```

### Масштабирование (если необходимо)

```bash
# Запуск нескольких экземпляров scheduler-srv
docker-compose up -d --scale scheduler-srv=2
```

## 📚 API документация

### Web интерфейс

После запуска всех сервисов, веб-интерфейс доступен по адресу:

- **Web интерфейс**: `http://localhost:3000`

Веб-интерфейс предоставляет полный функционал для работы с системой:
- Регистрация и авторизация пользователей
- Управление запланированными расходами (добавление, редактирование, удаление)
- Просмотр истории расходов с фильтрацией по датам
- Интуитивно понятный интерфейс

### Swagger UI

Также доступна Swagger документация для API в режиме Development:

- **Authorization Service**: `http://localhost:7069/swagger`
- **Expenses Scheduler Service**: `http://localhost:7236/swagger`

### Основные endpoints (через Gateway)

**Base URL**: `http://localhost:8082`

#### Авторизация

- `POST /Authorization/Authenticate` - Аутентификация пользователя
- `POST /Authorization/Register` - Регистрация нового пользователя
- `GET /Authorization/ConfirmEmail?userID={guid}` - Подтверждение email

#### Управление расходами

- `GET /ExpensesScheduler/GetScheduledExpenses` - Получить список запланированных расходов
- `POST /ExpensesScheduler/AddScheduledExpense` - Добавить запланированный расход
- `PUT /ExpensesScheduler/UpdateScheduledExpense` - Обновить запланированный расход
- `DELETE /ExpensesScheduler/DeleteScheduledExpense` - Удалить запланированный расход
- `GET /ExpensesScheduler/GetExpensesHistory?start={date}&end={date}` - Получить историю расходов

**Примечание**: Все endpoints `/ExpensesScheduler/*` требуют JWT токен в заголовке `Authorization: Bearer {token}`

## 💡 Примеры использования

### 1. Регистрация пользователя

```bash
curl -X POST http://localhost:8082/Authorization/Register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "SecurePassword123!"
  }'
```

**Ответ:**
```json
{
  "statusCode": 200,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "errorMessage": null
}
```

### 2. Аутентификация

```bash
curl -X POST http://localhost:8082/Authorization/Authenticate \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "SecurePassword123!"
  }'
```

**Ответ:**
```json
{
  "statusCode": 200,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "errorMessage": null
}
```

**Примечание**: Сначала нужно подтвердить email через `/Authorization/ConfirmEmail?userID={guid}`

### 3. Добавление запланированного расхода

```bash
curl -X POST http://localhost:8082/ExpensesScheduler/AddScheduledExpense \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "description": "Аренда квартиры",
    "amount": 50000,
    "scheduleType": 2,
    "oneTimeOnly": false
  }'
```

**Параметры scheduleType:**
- `0` - PerDay (ежедневно)
- `1` - PerWeek (еженедельно)
- `2` - PerMonth (ежемесячно)

**Альтернатива:** Можно использовать `happensInDays` вместо `scheduleType` для произвольного периода.

### 4. Получение запланированных расходов

```bash
curl -X GET http://localhost:8082/ExpensesScheduler/GetScheduledExpenses \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

**Ответ:**
```json
{
  "statusCode": 200,
  "userID": "guid",
  "scheduledExpenses": [
    {
      "id": "guid",
      "description": "Аренда квартиры",
      "amount": 50000,
      "createdDate": "2024-01-01T00:00:00Z",
      "happensInDays": null,
      "oneTimeOnly": false,
      "everyMonth": true
    }
  ]
}
```

### 5. Получение истории расходов

```bash
curl -X GET "http://localhost:8082/ExpensesScheduler/GetExpensesHistory?start=2024-01-01&end=2024-01-31" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### 6. Обновление запланированного расхода

```bash
curl -X PUT http://localhost:8082/ExpensesScheduler/UpdateScheduledExpense \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "expenseID": "guid",
    "description": "Обновленное описание",
    "amount": 55000,
    "scheduleType": 2
  }'
```

### 7. Удаление запланированного расхода

```bash
curl -X DELETE http://localhost:8082/ExpensesScheduler/DeleteScheduledExpense \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "expenseID": "guid"
  }'
```

## 🔧 Особенности реализации

### Паттерны проектирования

- **API Gateway Pattern**: Единая точка входа через YARP
- **Decorator Pattern**: `CachingSheduledExpensesServiceDecorator` для кэширования
- **Repository Pattern**: Работа с данными через DbContext с extension-методами
- **Producer-Consumer Pattern**: Асинхронная коммуникация через Kafka
- **Dependency Injection**: Полное использование DI контейнера .NET

### Оптимизация производительности

- **Redis кэширование**: Кэширование запланированных расходов и истории
- **Индексы БД**: Оптимизированные индексы для частых запросов
- **AsNoTracking**: Для операций только чтения
- **Distributed Cache**: Кэширование истории с автоматическим расширением диапазона

### Безопасность

- **JWT токены**: Стандартная JWT авторизация
- **BCrypt хеширование**: Безопасное хранение паролей
- **Middleware авторизации**: Централизованная проверка токенов
- **Email подтверждение**: Двухэтапная регистрация

### Фоновые задачи

- **Hangfire**: Планировщик фоновых задач
- **Ежедневная обработка**: Автоматическая проверка запланированных расходов в 6:00 UTC
- **Асинхронная отправка уведомлений**: Через Kafka для надежности

### Надежность

- **Health Checks**: Мониторинг состояния всех сервисов
- **Обработка ошибок**: Graceful degradation при проблемах с кэшем
- **Транзакции БД**: Гарантия целостности данных
- **Retry механизмы**: Для Kafka consumers

## 🗄 Базы данных

Проект использует несколько баз данных SQL Server:

- **Users**: Хранение пользователей и учетных данных
- **ExpensesDb**: Запланированные расходы и история операций
- **Hangfire**: Таблицы для фоновых задач Hangfire
- **SmtpNotificationSettings**: Настройки email-уведомлений пользователей

Все базы данных автоматически создаются при первом запуске (через `Database.EnsureCreated()`).

## 🔍 Мониторинг и отладка

### Hangfire Dashboard

В режиме Development доступен Hangfire Dashboard:
- URL: `http://localhost:7236/hangfire`

### Логи

Все сервисы логируют важные события:
```bash
# Логи конкретного сервиса
docker-compose logs -f scheduler-srv

# Логи за последний час
docker-compose logs --since 1h scheduler-srv
```

### Kafka топики

Для проверки сообщений в Kafka можно использовать Kafka CLI инструменты (требуется установка Kafka отдельно или использование контейнера).

## 🚧 Известные ограничения

- SMTP сервис требует настройки SMTP сервера в конфигурации (по умолчанию не настроен)
- Email подтверждение требует ручного вызова endpoint (в продакшене должен быть автоматизирован)
- Kafka топики создаются автоматически, но рекомендуется предварительное создание для production

## 📝 Лицензия

См. файл [LICENSE.txt](LICENSE.txt)

## 👤 Автор

Pet-проект для портфолио

---

**Примечание**: Это демонстрационный проект, созданный для изучения микросервисной архитектуры и современных технологий .NET.
